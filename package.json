{
  "name": "valuador-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Valuador inmobiliario — landing + API leyendo MongoDB Atlas",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "install:frontend": "npm install --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "dev:backend": "node --watch backend/server.js",
    "dev:frontend": "npm run dev --prefix frontend",
    "test": "node --test backend/*.test.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "mongodb": "^6.10.0"
  },
  "comment_optionalDependencies": "Para activar la lectura de copia literal, instalar en el servidor: npm i multer pdfjs-dist @napi-rs/canvas tesseract.js  (son opcionales; sin ellas /api/copia-literal responde 503 y el resto funciona).",
  "engines": {
    "node": ">=20"
  }
}
